namespace StreetFighter.Gameplay
{
    /// <summary>水平方向 / 屏幕边界方位。</summary>
    public enum Side
    {
        /// <summary>不在任何一侧（未贴边）。</summary>
        None = 0,

        Left = 1,

        Right = 2,
    }
}
