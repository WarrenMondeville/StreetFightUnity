namespace StreetFighter.Core
{
    /// <summary>
    /// 写在代码里的固定音效路径（Resources/Sound 下的相对路径）。
    /// 角色动作音效来自 config.json，不在本类中。
    /// </summary>
    public static class SoundPaths
    {
        public const string Fall = "sound/fall.mp3";
        public const string Defense = "sound/defense.mp3";
        public const string HitHeavyBoxing = "sound/hit_heavy_boxing.mp3";
        public const string BackgroundMusic = "sound/china.mp3";
    }
}
